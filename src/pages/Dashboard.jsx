// Bit 6 will fill this in: progress stats + CSV export of the review_logs trail.
export default function Dashboard() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Dashboard</h2>
      <p className="mt-2 text-sm text-slate-500">
        Coming in Bit 6 — review stats and CSV export.
      </p>
    </section>
  )
}
