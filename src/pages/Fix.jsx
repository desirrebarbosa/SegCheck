// Bit 5 will fill this in: the Canvas pixel-clip tool that trims mask pixels
// sticking outside the ground-truth bbox.
export default function Fix() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Fix</h2>
      <p className="mt-2 text-sm text-slate-500">
        Coming in Bit 5 — manual pixel-clip on a Canvas.
      </p>
    </section>
  )
}
